{
  "name": "mock",
  "version": "1.0.0",
  "description": "app-mock-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json-server": "json-server --watch data.json --port 5000"
  },
  "repository": {
    "type": "git"
  },
  "author": "sunn",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.1"
  }
}
